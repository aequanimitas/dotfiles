[alias]
  c    = commit
  cv   = commit -v
  lsoe = ls-files --others --exclude-standard
  l    = log
  lp   = log -p
  lodg = log --oneline --decorate --graph
  stu   = status -u
  lsoex = ls-files --others --exclude-standard
